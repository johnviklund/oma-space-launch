# TODO — intake

Ideas between runs, not yet brainstormed or scoped. **Don't implement something just because it's
listed here** — it moves to `ROADMAP.md` (or gets folded into a run) only after a `workflow
brainstorm` or `workflow todo` pass.

## Ideas

(none yet)

## Open Questions

- Exact QML component reuse strategy vs. Clock/Weather (share components or duplicate the
  pattern?) — defer to Phase 2 planning of the first roadmap initiative.
- Data refresh cadence/backoff against the Launch Library 2 API (rate limits, polling interval) —
  defer to Phase 2 planning.
